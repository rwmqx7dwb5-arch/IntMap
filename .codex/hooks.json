{
  "description": "⚠ GENERATED from .agents/session-start.json by `node scripts/agent-sync.mjs --write` — do not edit. IntMap — what a session is told at startup, in both products: branch, uncommitted changes, the free round number, last night's deep-tier verdict, and the accumulated memory (#R696). ⚠ Codex records hook trust against the hook's HASH — a new or edited hook is listed but SKIPPED until it is trusted once with /hooks (docs/AGENT-SETUP.md §7).",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|clear",
        "hooks": [
          {
            "type": "command",
            "command": "node scripts/worktree.mjs status --brief",
            "statusMessage": "IntMap · reading the working state",
            "timeout": 20,
            "additionalContextLimit": 2000
          },
          {
            "type": "command",
            "command": "node scripts/agent-memory.mjs --budget 36000",
            "statusMessage": "IntMap · reading the accumulated memory",
            "timeout": 20,
            "additionalContextLimit": 40000
          },
          {
            "type": "command",
            "command": "node scripts/agent-memory.mjs --check",
            "statusMessage": "IntMap · measuring the memory index against the ceiling that cuts it",
            "timeout": 20,
            "additionalContextLimit": 500
          }
        ]
      }
    ]
  }
}
