{
  "name": "intmap-ops",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "IntMap — a Vite-built static site (#R175) plus the operational quality baseline around it (CI, static checks, browser smoke tests, internal-QA runner, release/rollback/monitoring tooling). `dependencies` are what the browser ships; `devDependencies` are build/CI only.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "//serve": "(#R175) `serve` still means 'give me the real site on 4173' as the standing docs say — it just builds first now. `preview` skips the build when dist/ is already current; `dev` is the HMR loop.",
    "serve": "npm run build && node scripts/serve.mjs --root dist",
    "preview": "node scripts/serve.mjs --root dist",
    "check:static": "node scripts/static-checks.mjs",
    "//check:engine": "(#R178) the renderer-decoupling gate: only js/geo-engine.js may name MapLibre. Parses, so a comment mentioning `map.` never trips it.",
    "check:engine": "node scripts/engine-coupling.mjs --gate",
    "//check:i18n": "(#R239) the ONE translation gate — every language against every surface it can be translated on (keyed table wherever it is declared, the inline L() table, the five positional arguments, the two reading pages, and the data-i18n keys the markup asks for). `--todo <code>` prints the commands that close a gap. Runs inside `npm test`.",
    "check:i18n": "node scripts/i18n-audit.mjs --gate",
    "//check:docs": "the cross-document gate: the facts written down in more than one place (what is served, how many Edge Functions there are, which languages exist, how production is deployed) are compared with the repository AND with each other. It also enforces that Architecture.md stays a current-state spec rather than a changelog.",
    "check:docs": "node scripts/doc-facts.mjs --check",
    "//check:catalog": "(#R278) the Atlas catalogue gate: every capability the dispatch in js/atlas-console.js can execute must be described to the planner in function SYS(). An action the catalogue does not describe does not exist for the planner (#R115) — six of them, the road-network isochrone included, were invisible until this gate was written. Runs inside `npm test`.",
    "check:catalog": "node scripts/atlas-catalog.mjs --check",
    "//check:perf": "(#R311) THE STARTUP BUDGET, and before it nothing in CI weighed a single byte of the deploy — `vite build` printed «Some chunks are larger than 3000 kB» on every run and exited 0. It reads .perf/build-report.json (written by the build itself, scripts/build-report.mjs) and judges the two halves separately: the EAGER set is ratcheted in BOTH directions (over the ceiling is a regression; well under it means the ceiling stopped asserting anything and must be lowered with --update, the same rule #R194 gave the test-time budget), while ASYNC chunks and the dist/ totals get a ceiling only, because a 4.8 MB Cesium chunk a MapLibre session never fetches is not a startup cost. Needs a build first, so it runs as its own CI step after `npm run build` rather than inside `npm test` (which already builds once, in the browser half, concurrently).",
    "check:perf": "node scripts/perf-budget.mjs",
    "//check:assets": "(#R322) THE OTHER HALF OF THE DEPLOY. check:perf weighs what Rollup produced; this weighs what was COPIED — data/ is 55.8 MB of a 105.7 MB tree and none of it passes through the bundler. Every file in dist/ is matched against the strings the source actually contains (js/ src/ css/ *.html sw.js, plus the small manifests under data/ that name assets the JavaScript never spells out), and a file nothing mentions, a payload shipped twice, or a file over the per-file ceiling with no recorded reason fails the gate. Needs a build first, so it runs beside check:perf rather than inside npm test.",
    "check:assets": "node scripts/asset-report.mjs --check",
    "//perf:report": "(#R311) the same measurement without the judging — eager vs async, raw/gzip/brotli, the largest eager modules, duplicated modules. `npm run build` writes it; this prints it.",
    "perf:report": "node scripts/build-report.mjs",
    "check:capabilities": "node scripts/atlas-capability-audit.mjs --check",
    "audit:capabilities": "node scripts/atlas-capability-audit.mjs --json",
    "//master": "(#R282) the master copy is the OneDrive working directory (CLAUDE.md §6), and until this existed no step in the workflow was responsible for it: a round could merge, deploy and back up to USB while OneDrive stayed fifteen commits behind. `master:sync` is the last step of every task; `master:check` is the gate the USB mirror runs first. NOT in `npm test` — on a CI runner the checkout is a detached PR ref and 'behind origin/main' is the correct state there. What CI can prove about the rule is in tests/r282-checks.test.mjs.",
    "master:check": "node scripts/master-sync.mjs --check",
    "master:sync": "node scripts/master-sync.mjs --sync",
    "test:smoke": "playwright test tests/smoke.spec.js",
    "test:qa": "playwright test tests/internal-qa.spec.js",
    "test:e2e": "playwright test",
    "//test:profile": "(#R184) the renderer PROFILING instrument — frame time beside a sharpness measure, swept over the anti-aliasing and LOD settings. Excluded from the default run (see playwright.config.js): it is a measurement, not a gate.",
    "//test:profile2": "(#R191) …and it STILL did not run. On Windows spawnSync('npx.cmd') without a shell cannot resolve the launcher, so it returned status null, `||0` turned that into a clean exit, and the command printed nothing at all and reported success — the same silence #R190 set out to fix, one layer down. `shell` on win32 makes the launcher resolvable, and a null status is now a FAILURE rather than a pass. Verified by running it: 1 passed (2.8m), with the sweep table on stdout.",
    "test:profile": "node -e \"process.env.IM_PROFILE='1';const{spawnSync}=require('child_process');const r=spawnSync(process.platform==='win32'?'npx.cmd':'npx',['playwright','test','--workers=1'],{stdio:'inherit',env:process.env,shell:process.platform==='win32'});if(r.error)throw r.error;process.exit(r.status==null?1:r.status)\"",
    "test:security": "node --test tests/security-logic.mjs",
    "test:monitor": "node --test tests/monitor-logic.test.mjs",
    "//test:checks": "(#R301) ONE HAND-MAINTAINED LIST, and a file left out of it never runs — so it is not a weaker test, it is not a test. tests/r210 and tests/r211 were both left out when they were written and had never once been executed; when #R301 ran them, five of r211's twelve tests failed — the earliest of them broken by #R212, ninety rounds before anybody saw it. scripts/check-test-list.mjs now compares this list with the disk in both directions. It runs from `check:static`, NOT from here, because a guard for a list cannot be an entry in the list it guards.",
    "test:checks": "node --test tests/security-logic.mjs tests/monitor-logic.test.mjs tests/r147-checks.test.mjs tests/r149-checks.test.mjs tests/r150-checks.test.mjs tests/r151-checks.test.mjs tests/r152-checks.test.mjs tests/r153-checks.test.mjs tests/r154-checks.test.mjs tests/r155-checks.test.mjs tests/r156-checks.test.mjs tests/r157-checks.test.mjs tests/r158-checks.test.mjs tests/r159-checks.test.mjs tests/r160-checks.test.mjs tests/r161-checks.test.mjs tests/r162-checks.test.mjs tests/r163-checks.test.mjs tests/r164-checks.test.mjs tests/r165-checks.test.mjs tests/r166-checks.test.mjs tests/r167-checks.test.mjs tests/r168-checks.test.mjs tests/r169-checks.test.mjs tests/r170-checks.test.mjs tests/r171-checks.test.mjs tests/r172-checks.test.mjs tests/r173-checks.test.mjs tests/r174-checks.test.mjs tests/r175-checks.test.mjs tests/r176-checks.test.mjs tests/r179-checks.test.mjs tests/r180-checks.test.mjs tests/r181-checks.test.mjs tests/r182-checks.test.mjs tests/r183-checks.test.mjs tests/r184-checks.test.mjs tests/r185-checks.test.mjs tests/r185b-checks.test.mjs tests/r186-checks.test.mjs tests/r187-checks.test.mjs tests/r188-checks.test.mjs tests/r189-checks.test.mjs tests/r190-checks.test.mjs tests/r191-checks.test.mjs tests/r192-checks.test.mjs tests/r193-checks.test.mjs tests/r195-checks.test.mjs tests/r196-checks.test.mjs tests/r198-checks.test.mjs tests/r199-checks.test.mjs tests/r200-checks.test.mjs tests/r197-checks.test.mjs tests/r197-space.test.mjs tests/r201-checks.test.mjs tests/r202-checks.test.mjs tests/r203-checks.test.mjs tests/r204-checks.test.mjs tests/r205-checks.test.mjs tests/r206-checks.test.mjs tests/r207-checks.test.mjs tests/r208-checks.test.mjs tests/r209-checks.test.mjs tests/r210-checks.test.mjs tests/r211-checks.test.mjs tests/r212-checks.test.mjs tests/r213-checks.test.mjs tests/r214-checks.test.mjs tests/r215-checks.test.mjs tests/r216-checks.test.mjs tests/r217-checks.test.mjs tests/r218-checks.test.mjs tests/r219-checks.test.mjs tests/r220-checks.test.mjs tests/r221-checks.test.mjs tests/r222-checks.test.mjs tests/r223-checks.test.mjs tests/r224-checks.test.mjs tests/r225-checks.test.mjs tests/r226-checks.test.mjs tests/r227-checks.test.mjs tests/r228-checks.test.mjs tests/r230-checks.test.mjs tests/r231-checks.test.mjs tests/r232-checks.test.mjs tests/r233-checks.test.mjs tests/r234-checks.test.mjs tests/r235-checks.test.mjs tests/r236-checks.test.mjs tests/r237-checks.test.mjs tests/r238-checks.test.mjs tests/r239-checks.test.mjs tests/r240-checks.test.mjs tests/r241-checks.test.mjs tests/r242-checks.test.mjs tests/r243-checks.test.mjs tests/r244-checks.test.mjs tests/r245-checks.test.mjs tests/r246-checks.test.mjs tests/r247-checks.test.mjs tests/r248-checks.test.mjs tests/r249-checks.test.mjs tests/r250-checks.test.mjs tests/r252-checks.test.mjs tests/r253-checks.test.mjs tests/r254-checks.test.mjs tests/r255-checks.test.mjs tests/r257-checks.test.mjs tests/r258-checks.test.mjs tests/r260-checks.test.mjs tests/r261-checks.test.mjs tests/r262-checks.test.mjs tests/r263-checks.test.mjs tests/r264-checks.test.mjs tests/r265-checks.test.mjs tests/r267-checks.test.mjs tests/r266-checks.test.mjs tests/r268-checks.test.mjs tests/r269-checks.test.mjs tests/r270-checks.test.mjs tests/r271-checks.test.mjs tests/r273-checks.test.mjs tests/r274-checks.test.mjs tests/r275-checks.test.mjs tests/r276-checks.test.mjs tests/r277-checks.test.mjs tests/r278-checks.test.mjs tests/r279-checks.test.mjs tests/r282-checks.test.mjs tests/r283-checks.test.mjs tests/r280-checks.test.mjs tests/r284-checks.test.mjs tests/r285-checks.test.mjs tests/r286-checks.test.mjs tests/r287-checks.test.mjs tests/r288-checks.test.mjs tests/r289-checks.test.mjs tests/r290-checks.test.mjs tests/r291-checks.test.mjs tests/r292-checks.test.mjs tests/r293-checks.test.mjs tests/r296-checks.test.mjs tests/r295-checks.test.mjs tests/r297-checks.test.mjs tests/r298-checks.test.mjs tests/r299-checks.test.mjs tests/r301-checks.test.mjs tests/r302-checks.test.mjs tests/r304-checks.test.mjs tests/r305-checks.test.mjs tests/r306-checks.test.mjs tests/r307-checks.test.mjs tests/r308-checks.test.mjs tests/r309-checks.test.mjs tests/r310-checks.test.mjs tests/r311-checks.test.mjs tests/r313-checks.test.mjs tests/r313b-checks.test.mjs tests/r314-checks.test.mjs tests/r318-checks.test.mjs tests/r320-checks.test.mjs tests/r322-checks.test.mjs tests/r323-checks.test.mjs tests/r325-checks.test.mjs tests/r333-checks.test.mjs tests/r336-checks.test.mjs",
    "//test": "(#R205) the source-level half (static checks, the engine gate, the budget, the ~60 node test files) and the browser half share nothing, so they run AT THE SAME TIME through scripts/test-parallel.mjs — npm test costs max(a,b) instead of a+b. (#R196) the browser half runs through scripts/run-tests.mjs, which orders the specs by their MEASURED time (longest first) and gives the specs that need a machine to themselves one worker — the same plan CI has used since #R195, applied to the local run. Same files, same config; it falls back to a plain `playwright test` if the plan cannot cover every spec.",
    "test": "node scripts/test-parallel.mjs",
    "//test:seq": "(#R205) the same steps in the old sequential order, for when an interleaved log is in the way of reading a failure.",
    "test:seq": "node scripts/static-checks.mjs && node scripts/engine-coupling.mjs --gate && node scripts/atlas-catalog.mjs --check && node scripts/test-budget.mjs && npm run test:checks && node scripts/run-tests.mjs && node scripts/perf-budget.mjs",
    "//test:deep": "(#R203) the half of the suite that does NOT stand in front of a push. (#R304) MEASURED: 59 spec files against the core tier's 6, because #R204/#R207 turned the split from a list into a PRICE (scripts/tiers.mjs, CORE_MAX_S) — a spec stands in the gate only if it costs at most one second, so nearly every per-round file is deep. Locally 55 of them run at two workers in ~23 min and the four `solo` Cesium files then run at one (~11 min). It runs NIGHTLY (ci.yml `schedule`) and on demand, NOT on push (#R207). Run it before opening a PR that touches 3-D, Cesium, the simulators or the physics. `test:all` is both. ⚠ scripts/tiers.mjs derives the split; tests/durations.json supplies the measured seconds and the `solo` list, not a `deep` list. ⚠ When it goes red nobody is watching by default — scripts/deep-alarm.mjs opens an issue and `node scripts/worktree.mjs status` prints last night's verdict (#R304).",
    "test:deep": "node -e \"process.env.IM_TIER='deep';import('./scripts/run-tests.mjs')\"",
    "test:all": "node -e \"process.env.IM_TIER='all';import('./scripts/run-tests.mjs')\"",
    "report": "playwright show-report",
    "check:testbudget": "node scripts/test-budget.mjs",
    "//check:testbudget": "(#R197) the suite has a CEILING and it only ever goes down — see scripts/test-budget.mjs. A round that adds test time must take at least as much out somewhere else; that is what stops the per-round spec files accumulating into 「何重にも」.",
    "//prebuild": "(#R232) js/locales/_langs.js is the language list for the two static reading pages (no bundler there, so no import.meta.glob). Regenerating it here is what makes 「adding a language = adding one locale file」 true for those pages too; tests/r232-checks fails if the committed copy is stale.",
    "prebuild": "node scripts/i18n-langs.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "acorn": "^8.17.0",
    "acorn-walk": "^8.3.5",
    "js-yaml": "4.3.1",
    "opencc-js": "^1.4.1",
    "opentype.js": "^2.0.0",
    "vite": "6.4.3"
  },
  "dependencies": {
    "@mapbox/vector-tile": "^2.0.3",
    "@supabase/supabase-js": "2.58.0",
    "@turf/along": "6.5.0",
    "@turf/area": "6.5.0",
    "@turf/bbox": "6.5.0",
    "@turf/bbox-clip": "6.5.0",
    "@turf/bearing": "6.5.0",
    "@turf/boolean-point-in-polygon": "6.5.0",
    "@turf/buffer": "6.5.0",
    "@turf/center": "6.5.0",
    "@turf/centroid": "6.5.0",
    "@turf/circle": "6.5.0",
    "@turf/convex": "6.5.0",
    "@turf/distance": "6.5.0",
    "@turf/great-circle": "6.5.0",
    "@turf/helpers": "6.5.0",
    "@turf/kinks": "6.5.0",
    "@turf/length": "6.5.0",
    "@turf/point-on-feature": "6.5.0",
    "@turf/turf": "6.5.0",
    "@turf/union": "6.5.0",
    "cesium": "^1.143.0",
    "html2canvas": "1.4.1",
    "katex": "0.16.21",
    "maplibre-contour": "0.1.0",
    "maplibre-gl": "5.24.0",
    "pbf": "^4.0.1",
    "polygon-clipping": "0.15.7",
    "satellite.js": "^7.1.0",
    "topojson-client": "3.1.0"
  },
  "allowScripts": {
    "esbuild@0.25.12": true
  },
  "overrides": {
    "dompurify": "3.4.13",
    "nanoid": "3.3.18"
  }
}
