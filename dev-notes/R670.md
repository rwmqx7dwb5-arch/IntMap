## R670 — **#R666 の入口は本番で 0×0 だった——そして同じ指示に同じ間違った答えを返したのは、これが 2 回目**

〈#R666 の production verification。**ボタンも OS アクションも正しく本番に届いていた**——
`document.getElementById('btn-pandemic-sim')` は実在し、ラベルは `Pandemic Simulator`、
`IntMapOS.has('sim.pandemic')` は true、`IntMapOS.exec('sim.pandemic')` は `#pg-pan-hud` を開いた
（ハブではなく）。それでも依頼は満たされていなかった〉

### 1. ⚠⚠⚠ 帯ごと描かれていなかった

本番実測（R666・R667 の両方、デスクトップ 1280×720 とモバイル 375×812）:

```
getComputedStyle(#layer-tools).display === 'none'
#btn-pandemic-sim .getBoundingClientRect() → 全辺 0
```

`#layer-tools` は **`#layer-dropdown` の中**にあり、それは**クラシックのレイヤードロップダウン**である。
`imLayerPanel` は **#R154 以降ずっと `'right'` が既定**なので、既定の読者はその帯を一度も見ない。
同じ帯にいる `btn-compare` / `btn-correlate` / `btn-upload-geojson` / `btn-seismic-sim` /
**`btn-edu`（Playground ハブ）** も全部 0×0 だった。

読者が Layers を開いて見る Tools 一覧は **10 行**で、それは `js/map-ui.js` の別のレジストリから
描かれている（`Directions / Photo location / Earthquake simulator / Terrain & water /
Radioactive plume / Radio coverage & line of sight / Reachable area / Sunlight hours /
Night sky / Drone flight planner`）。パネルの検索欄に `pandemic` と入れて **0 件**、`playground` も **0 件**。

### 2. ⚠⚠⚠ そして、その警告は私が読んだファイルの中に書いてあった

`js/map-ui.js` のツール CSS の直前:

> #R242 answered this by appending a button to `#layer-tools`, which lives inside `#layer-dropdown`
> — the CLASSIC dropdown, and `imLayerPanel` has defaulted to `'right'` since #R154. MEASURED on the
> shipped build: the button exists, `display:block`, and its bounding rect is 0×0 … So the
> instruction was re-sent, and correctly.

**同じ指示に、同じ間違った答えを返したのは 2 回目**である（#R242 → #R243、そして #R666 → この回）。
私は `_seisBtn` を「先例」として真似たが、真似るべきだったのは**そのあとに来た訂正**のほうだった。

### 3. ⚠⚠⚠ 手で書いた一覧は、そこに足されなかったものに気づけない

`tests/r258-checks` ⑨ と `tests/r261-checks` ⑨ は、まさにこの不変条件——
**「レイヤーでない模擬装置は Tools 一覧の行である」**——を守るために書かれている。
どちらも**緑のままだった。** 測り方が

```js
['sim.seismic','sim.terrainWater','sim.radiation', … ].forEach(id => assert.ok(s.includes("id:'"+id+"'")))
```

＝**手で書いた id の一覧**だからである。一覧に無いものは、一覧からは見えない
（`.agents/rules/no-ad-hoc-hardcoding.md` §2 の 4「一覧が要るなら、一覧を発見する」）。

⇒ `tests/r670-checks.test.mjs` ① は一覧を持たない。**`js/app-body.js` が
`IntMapOS.register('sim.…')` する模擬装置を数え上げて**、そのすべてに `js/map-ui.js` の行を要求する。
⚠ **変異試験で確かめた**——行を消すと 3 本とも落ち、戻すと 3 本とも通る。

### 4. 行そのもの

`js/map-ui.js` のツール一覧に `sim.pandemic` を追加（`run:null`＝開くのは OS アクション、
`mod:'IntMapPandemic'`＝走行中は点灯し 2 度目の押下で閉じる #R264、9 言語の名前と説明、検索語）。
`js/playground.js` が `window.IntMapPandemic`（`isOpen` / `close`）を**自分の扉の中から**公開する
——描いただけで遅延モジュールが読まれてはならない（#R209）ので、開かれるまでこの global は無く、
行は消灯している。それが事実である。

⚠ **`#btn-pandemic-sim` は消さない。** `imLayerPanel` の `classic` はいまも設定として在り、
その設定で描かれるのが `#layer-tools` である（地震シミュレーターも同じ理由で両方を持つ）。
変わったのは**どちらが「レイヤー欄から開ける」の主張なのか**であって、ボタンの有無ではない。

### 5. 本番検証で緑だったもの（#R666 のうち、入口以外）

- `data/country-facts.json` **200 / 65,140 B**・`data/airports.json` **200 / 13,442 B** を実際に取得
- HUD の注記は `International spread is weighted by population, land borders and airport capacity
  — not by flight routes or passenger numbers.`（表が読めなかったときの文面ではない）
- `pg-dots` の全 feature に `id` が付いている・`cls` は 0/1 の 2 値
- `pg-dots-heat` は `maxzoom 3.2`。**z2.2 で 701 件描画・z5 で 0 件**（点は残る）
- **凡例の整合**（#R666 が直した「60 倍」）: `1 dot ≈ N` × 点数 と 現感染者数の比は
  **1.014 / 1.002 / 1.001**（3 標本）
- `window.__imErrors` **0 件**

### 6. 残っている

- ⚠ **Atlas の能力索引（138 件）に pandemic が 1 件も無い**（`sim.radiation` / `sim.earthquake` /
  `sim.space` / `sim.tsunami` / `sim.ballistic` だけ）。`IntMapOS.catalog().commands` には
  `sim.pandemic` が入っているが、**日本語の依頼から到達できるかは未ログインでは測れなかった**
  （Atlas の回答生成は 401）。[[intmap-atlas-capability-index]] の形——**索引に無い能力のために扉は
  開かれない**——なので、次のラウンドで能力表に載せるか、載せない理由を書くか。
- 2 通目の監査の残り（#R666 §9）。

